<?xml version="1.0" encoding="UTF-8" ?>
<class name="GameInputMapper" inherits="Node" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		Bridges a [GameInputActionMap] onto Godot's [code]InputMap[/code] each frame.
	</brief_description>
	<description>
		[GameInputMapper] is a [Node] that resolves a target [GameInputDevice] (either by id via [member target_device_id] or as the primary device of [member target_kind_mask]) every [code]_process[/code] frame, fetches its [GameInputReading], and walks [member action_map] calling [code]Input.action_press(action, strength)[/code] or [code]Input.action_release(action)[/code] on each press / release edge so the rest of your game can keep using the standard [code]Input.is_action_pressed("jump")[/code] APIs without knowing GameInput exists. On every press / release transition the mapper additionally pushes an [InputEventAction] through [code]Input.parse_input_event[/code] so event-driven listeners — Viewport GUI focus traversal for [code]ui_*[/code] actions, [code]_gui_input[/code] handlers, [code]_input[/code] / [code]_unhandled_input[/code] — actually see the action fire, since [code]Input.action_press[/code] alone updates polled state but does not deliver an [InputEvent].
		Bindings can use any [enum GameInputDevice.Source]: gamepad buttons and axes, arcade stick, flight stick and racing wheel buttons, and racing wheel and flight stick axes. Point [member target_kind_mask] at the matching [enum KindFlags] (for example [constant KIND_RACING_WHEEL]) or set [member target_device_id]. Keyboards and mice have no [enum GameInputDevice.Source] values; bind them with regular [InputEventKey] and [InputEventMouseButton] events in the [code]InputMap[/code], or read them from [GameInputReading]. [constant KIND_KEYBOARD] and [constant KIND_MOUSE] are kept for target selection.
		To avoid double-firing when Godot's built-in joypad backend is already wired to deliver the same action via an [InputEventJoypadButton] / [InputEventJoypadMotion] in the project's [code]InputMap[/code] (e.g., the default [code]ui_accept[/code] mapping that includes joypad button A), the mapper checks each binding once against the action's [code]InputMap[/code] events and skips its own synthetic [InputEventAction] when a matching native joypad event is found. The polled state via [code]Input.action_press[/code] is still refreshed every frame either way. The check is per-binding, cached, invalidated whenever [member action_map] or a binding on that map changes, and refreshed once per frame so runtime [code]InputMap[/code] edits made after the cache fills tolerate at most one frame of staleness. Gamepad face, shoulder, stick, D-pad, Menu, View and paddle buttons and the thumbstick and trigger axes take part in this check; other sources have no Godot joypad equivalent.
		The mapper calls [method GameInput.poll] defensively at the start of each frame; that call is per-frame idempotent so multiple [GameInputMapper] instances in the same scene do not refresh device state more than once per frame.
		Action names listed in [member GameInputBinding.action] must already exist in the project's [code]InputMap[/code]. Missing actions emit a single [code]push_warning[/code] per [GameInputMapper] instance per action (debounced via an internal set) and the binding is then ignored. Whenever the mapper stops driving a held binding — exiting the tree, swapping or mutating the active map, retargeting the mapper, losing the target device, or failing to obtain a fresh reading — it releases every action it previously held so Godot's polled action state cannot get stuck pressed.
	</description>
	<tutorials></tutorials>
	<methods>
		<method name="get_action_map" qualifiers="const">
			<return type="GameInputActionMap" />
			<description>
				Returns the currently assigned action map. See [member action_map].
			</description>
		</method>
		<method name="set_action_map">
			<return type="void" />
			<param index="0" name="map" type="GameInputActionMap" />
			<description>
				Assigns a new action map. Any actions held by the previous map are released before the internal previous-pressed state, native-event cache, and missing-action warning set are reset so the new map gets a clean slate.
			</description>
		</method>
		<method name="get_target_kind_mask" qualifiers="const">
			<return type="int" />
			<description>
				Returns the kind mask used when [member target_device_id] is [code]-1[/code]. See [member target_kind_mask].
			</description>
		</method>
		<method name="set_target_kind_mask">
			<return type="void" />
			<param index="0" name="mask" type="int" />
			<description>
				Sets the kind mask used to pick a primary device. If the mapper was holding any actions for the previous target scope, they are released before the mask changes. See [member target_kind_mask].
			</description>
		</method>
		<method name="get_target_device_id" qualifiers="const">
			<return type="int" />
			<description>
				Returns the explicitly targeted device id, or [code]-1[/code] when targeting the primary device of [member target_kind_mask]. See [member target_device_id].
			</description>
		</method>
		<method name="set_target_device_id">
			<return type="void" />
			<param index="0" name="id" type="int" />
			<description>
				Sets a specific [GameInputDevice] id to drive. If the mapper was holding any actions for the previous device, they are released before the id changes. Pass [code]-1[/code] to revert to "primary device of [member target_kind_mask]". See [member target_device_id].
			</description>
		</method>
		<method name="get_active_binding_count" qualifiers="const">
			<return type="int" />
			<description>
				Returns the number of bindings whose action is currently being held pressed by this mapper. Useful for tests and HUD-style debugging.
			</description>
		</method>
	</methods>
	<members>
		<member name="action_map" type="GameInputActionMap" setter="set_action_map" getter="get_action_map">
			The [GameInputActionMap] this mapper iterates each frame. When [code]null[/code] the mapper is a no-op.
		</member>
		<member name="target_kind_mask" type="int" setter="set_target_kind_mask" getter="get_target_kind_mask" default="1">
			Bitfield of [enum KindFlags] values used to pick the primary device when [member target_device_id] is [code]-1[/code]. Defaults to [constant KIND_GAMEPAD]. Despite its enum, the field accepts an OR-ed combination so a mapper can be retargeted at, for example, [code]KIND_GAMEPAD | KIND_RACING_WHEEL[/code]; the first connected device matching any of the flags is used.
		</member>
		<member name="target_device_id" type="int" setter="set_target_device_id" getter="get_target_device_id" default="-1">
			Specific [GameInputDevice] id to drive, or [code]-1[/code] (the default) to use the primary device matching [member target_kind_mask]. The id is looked up whatever the device's kind. Use this for split-screen / per-player mapper instances by storing the id reported by [signal GameInput.device_connected].
		</member>
	</members>
	<constants>
		<constant name="KIND_GAMEPAD" value="1" enum="KindFlags">
			Bit flag for gamepad-style devices. Mirrors [constant GameInput.DEVICE_GAMEPAD] and is the default value of [member target_kind_mask].
		</constant>
		<constant name="KIND_KEYBOARD" value="2" enum="KindFlags">
			Bit flag for keyboard devices. Mirrors [constant GameInput.DEVICE_KEYBOARD]. Keyboards have no [enum GameInputDevice.Source] values, so no binding reads them; use [InputEventKey] events in the [code]InputMap[/code] instead.
		</constant>
		<constant name="KIND_MOUSE" value="4" enum="KindFlags">
			Bit flag for mouse devices. Mirrors [constant GameInput.DEVICE_MOUSE]. Mice have no [enum GameInputDevice.Source] values, so no binding reads them; use [InputEventMouseButton] events in the [code]InputMap[/code] instead.
		</constant>
		<constant name="KIND_ARCADE_STICK" value="8" enum="KindFlags">
			Bit flag for arcade sticks. Mirrors [constant GameInput.DEVICE_ARCADE_STICK]; bind them with the [code]SRC_ARCADE_*[/code] sources.
		</constant>
		<constant name="KIND_FLIGHT_STICK" value="16" enum="KindFlags">
			Bit flag for flight sticks. Mirrors [constant GameInput.DEVICE_FLIGHT_STICK]; bind them with the [code]SRC_FLIGHT_*[/code] and [code]SRC_AXIS_FLIGHT_*[/code] sources.
		</constant>
		<constant name="KIND_RACING_WHEEL" value="32" enum="KindFlags">
			Bit flag for racing wheels. Mirrors [constant GameInput.DEVICE_RACING_WHEEL]; bind them with the [code]SRC_WHEEL_*[/code] sources and the wheel, pedal and handbrake axes.
		</constant>
	</constants>
</class>
